
<div class="card-container">
    <div class="cards">
        {{#each cars}}
        <card-element photo={{this.photo}} title="{{this.title}}" equip="{{this.equip}}" price="{{this.price}}"></card-element>
        {{/each}}
    </div>
</div>

<script src="../public/components/card.js"></script>